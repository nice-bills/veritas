[project]
name = "veritas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cdp-sdk>=1.37.0",
    "eth-abi>=5.2.0",
    "pydantic>=2.12.5",
]
